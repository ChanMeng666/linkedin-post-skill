# Copy this file to `.env` and fill in your key. `.env` is gitignored.
# Used by scripts/gen-image.mjs for OpenAI image generation.
OPENAI_API_KEY=sk-...

# Optional overrides:
# OPENAI_IMAGE_MODEL=gpt-image-1
